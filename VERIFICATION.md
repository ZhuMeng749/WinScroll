# 本次验证

## 1.0.2 窗口打开修复

- 已编译、通过严格签名检查并安装到 `/Applications/WinScroll.app`。
- 已通过 Finder 的“打开”操作启动，确认设置窗口显示版本 1.0.2。
- Finder 启动时发现系统未认可新版的辅助功能权限，界面正确显示等待授权；更新后需要重新添加当前应用。
- 登录项启动与重新登录仍未实机验证。

## 1.0.1 本地修复复查

- 检查旧版运行进程时，偏好中的反转开关已开启，但系统事件过滤器列表中没有该进程。这说明开关状态不能作为服务已启动的证据；未据此断言具体权限失败原因。
- 实际采集普通蓝牙鼠标滚轮事件，确认 continuous、scroll phase、momentum phase 均为零，未被触控板保护逻辑排除；真实事件带有 IOHID 滚动负载，而旧版合成事件测试不包含该负载。
- 补充 IOHID 同步、权限提示、运行计数，并通过 34 项检查。其中 IOHID 测试使用本地创建的底层对象，不向桌面发送合成事件。
- 1.0.1 已编译并安装到 `/Applications/WinScroll.app`，签名检查通过。
- 本次诊断启动中，应用报告辅助功能可用、反转开关开启、事件过滤器已运行；已实际收到 71 次滚轮事件，反转及底层同步计数均为 71。
- 用户已确认页面上的鼠标滚动方向恢复正常；通过 Finder 重新启动、重新登录与更多设备兼容性未在本轮完成验证。
- 1.0.1 作为修复预览版发布；下方为 1.0.0 的历史验证记录。

## 1.0.0 历史验证

日期：2026-09-15。环境：Apple Silicon，macOS 26.6.2，Apple Swift 6.3.3。

- 使用兼容的 MacOSX26.5 SDK 完成 release 编译。
- 独立行为检查：17 项通过，覆盖垂直轴三种位移表示、水平轴保留、连续事件保留、手势与惯性保留、停用、不相关事件、正值/负值/零值。
- Info.plist 格式检查通过。
- 应用本地 ad-hoc 签名与严格签名检查通过。
- 应用预览进程成功启动。
- 已通过应用自己的离屏视图导出并检查中文界面，见 `docs/images/preview.png`。这张图是界面渲染预览，不是桌面截图。

### 1.0.0 发布时尚未验证的项目（历史记录）

本次检查未自动点击菜单栏、授予辅助功能权限或变更登录项；测试生成的滚动事件没有发送到桌面。

真实鼠标的系统级滚动效果、触控板手感、权限授予后的激活、开机启动及重新登录行为，需用户在目标 Mac 授权后验证。应用中已提供测试区与状态提示。

发布包为 arm64 预览版，未做 Apple 公证。
